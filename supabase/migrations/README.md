# Supabase Migrations

New SQL migrations should be added here using:

```text
YYYYMMDDHHMM_description.sql
```

The older `supabase/step-next-*.sql` files remain as historical checkpoints. New database changes should use this dated folder and should be recorded in `docs/APPLIED_MIGRATIONS.md` after they are applied and verified.
